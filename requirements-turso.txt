# NOTE: libsql-client ab requirements.txt mein bhi shamil hai (isliye
# ke Streamlit Cloud sirf requirements.txt se install karta hai — ye
# alag file kabhi install hi nahi hoti thi, jis se Turso Secrets set
# karte hi "ModuleNotFoundError: libsql_client" aata tha aur poora app
# crash ho jata tha). Ye file ab sirf local-dev documentation ke liye
# rakhi hai; deploy ke liye sirf requirements.txt kaafi hai.

libsql-client>=0.3.1
